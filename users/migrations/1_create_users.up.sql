CREATE TABLE users (id SERIAL PRIMARY KEY, username TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL, credits DOUBLE PRECISION DEFAULT 0, plan_type TEXT DEFAULT 'STARTER', created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP);
